import { WindowInitialState } from '@Common/types';
import { BaseScreen } from '@Main/screens';

export class SettingScreen extends BaseScreen {
  constructor(props: WindowInitialState) {
    super(props);
  }
}
